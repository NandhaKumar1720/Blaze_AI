{
  "name": "chatbot-server",
  "version": "1.0.0",
  "description": "High-performance Express server with clustering for AI chatbot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "cluster": "node server.js", 
    "prod": "pm2 start server.js -i max --name chatbot-server" 
  },
  "dependencies": {
    "axios": "^1.6.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^7.0.0",
    "os": "^0.1.2" 
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "pm2": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
